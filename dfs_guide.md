DFS:

Time → O(n)

Space → O(height)

BFS:

Time → O(n)

Space → O(width) → worst case O(n)

DFS - Traversals

We use BFS when we care about:

Levels

Shortest path (in graphs)

Minimum steps

Layered structure
